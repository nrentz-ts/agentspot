// ─── Product noun labels ─────────────────────────────────────────────────────
// Change these two strings to rename the concepts across the entire product.

export const AGENT_LABEL    = "Agent";      // singular  → e.g. "Agent", "Helper", "Skill"
export const WORKFLOW_LABEL = "Workflow";   // singular  → e.g. "Workflow", "Automation", "Flow"

// Derived helpers — no need to edit these
export const AGENTS_LABEL   = `${AGENT_LABEL}s`;
export const WORKFLOWS_LABEL = `${WORKFLOW_LABEL}s`;
